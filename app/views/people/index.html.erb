<h1>People</h1>

<ul>
  <% @people.each do |person| %>
  <li><%= link_to person.name, person %> owes <%= number_to_currency(person.owes, unit: "$") %></li>
  <% end %>
</ul>

<p>
  <%= link_to "Home", root_path %>
</p>
